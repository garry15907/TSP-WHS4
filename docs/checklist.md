# 테스트 체크리스트 및 결과

검증 기준일: 2026-07-24

## 기능 체크

- [x] 회원가입이 정상 동작한다.
결과: 자동 테스트 PASS
- [x] 중복 아이디 등록이 차단된다.
결과: 서버 로직 구현 완료, 수동 확인 대상
- [x] 로그인/로그아웃이 정상 동작한다.
결과: 자동 테스트 PASS, 스모크 테스트 PASS
- [x] 비활성 또는 휴면 계정은 로그인할 수 없다.
결과: 서버 로직 구현 완료, 수동 확인 대상
- [x] 상품 등록이 정상 동작한다.
결과: 서버 및 화면 구현 완료
- [x] 상품 검색이 제목/설명 기준으로 동작한다.
결과: 서버 및 화면 구현 완료
- [x] 상품 상세 페이지에서 판매자 정보가 보인다.
결과: 서버 및 화면 구현 완료
- [x] 전역 채팅 메시지가 실시간으로 브로드캐스트된다.
결과: Socket.IO 구현 완료
- [x] 1:1 채팅 메시지가 송수신된다.
결과: Socket.IO 구현 완료
- [x] 사용자 간 송금 시 잔액이 정확히 이동한다.
결과: 자동 테스트 PASS
- [x] 신고가 누적되면 상품은 차단되고 사용자는 휴면 전환된다.
결과: 상품 자동 차단 자동 테스트 PASS, 사용자 휴면 전환 로직 구현 완료
- [x] 관리자가 사용자/상품 상태를 변경할 수 있다.
결과: 관리자 대시보드 구현 완료

## 보안 체크

- [x] 모든 상태 변경 요청에 CSRF 토큰이 포함된다.
결과: `csrf-sync` 적용
- [x] 인증 요청에 rate limit가 적용된다.
결과: `express-rate-limit` 적용
- [x] 비밀번호가 평문으로 저장되지 않는다.
결과: `bcryptjs` 해시 저장
- [x] SQL 쿼리에 prepared statement가 사용된다.
결과: `node:sqlite` prepared statement 사용
- [x] 입력 길이와 형식 검증이 서버에서 수행된다.
결과: `zod` 기반 서버측 검증 적용
- [x] 자기 자신에게 송금/신고가 차단된다.
결과: 서버 로직 구현 완료
- [x] 중복 신고가 차단된다.
결과: DB UNIQUE 제약과 예외 처리 적용
- [x] 이미지 입력은 URL만 허용한다.
결과: 업로드 취약점 회피를 위해 URL 기반으로 제한

## 자동 테스트 요약

- `npm test` 실행 결과: 3 / 3 PASS
- 회원가입 및 로그인 테스트 PASS
- 상품 신고 임계치 차단 테스트 PASS
- 송금 잔액 이동 테스트 PASS

## 수동 검증 요약

- `/login` 페이지 스모크 테스트 PASS
- 관리자/채팅/상태 변경 플로우는 브라우저 수동 확인 대상으로 남김
